spring:
  application:
    name: ScronAndTransaction

  profiles:
    active: prod

  datasource:
    url: jdbc:mysql://localhost:3306/schedule?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 110603
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
